#include "main.h"
/**
 * _strcat - function concatenates two strings
 * @dest: destination variable
 * @src: source variable
 * Return: string
 */
char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
	return (dest);
}
