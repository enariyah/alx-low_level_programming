#include "main.h"
/**
 * add - adds two integers
 *@a: the variable to be added
 *@b: another variable
 * Return: return the result
 */
int add(int a, int b)
{
	int res;

	res = a + b;
	return (res);
}
