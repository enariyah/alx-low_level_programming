#include <stdio.h>
/**
 * main - Entery is here
 * Description: a-z
 * Return: always return 0
 */
int main(void)
{
	int a;

	for (a = 97; a <= 122; a++)
	{
		putchar(a);
	}
	putchar(10);
	return (0);
}
