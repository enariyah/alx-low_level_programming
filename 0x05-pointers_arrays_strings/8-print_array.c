#include "main.h"
/**
 * print_array - prints arranys
 * @a: a variable
 * @n: variable
 * Return: none
 */
void print_array(int *a, int n)
{
	int a;
	for (a = 0; a < n; a++)
	{
		if (i == 0)
			printf("%d", a[a]);
		else
			printf(", %d", a[a]);
	}
	printf("\n");
}
